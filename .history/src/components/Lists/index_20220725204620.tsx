import { HStack, Text } from 'native-base';

export type ListProps = {
  id: string;
  name: string;
  when: string;
  status
}

type Props = {

}

export function Index() {
  return (
    <HStack>
      <Text></Text>
    </HStack>
  );
}